# react-noclip

```bash
npm install react-noclip
```

```typescript
useNoclip({
  log: () => console.log("console.log"),
  alert: () => alert("alert"),
  toggleTitle: () => setShowTitle(!showTitle),
});
```

Opens with ⌘+k
